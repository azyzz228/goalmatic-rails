
<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

<div class="flex flex-row justify-between items-center my-12">
    <p class="text-3xl text-slate-900 font-bold">Categories</p>
    <%= link_to "Add", new_category_path, class:"bg-sky-500 text-sky-100 px-5 py-3 rounded-md" %>
</div>

  <div class="min-w-full">
    <%= render @categories %>
  </div>
  
</div>
